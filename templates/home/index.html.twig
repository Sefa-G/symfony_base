{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Accueil{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper" style="text-align:center">
    <h1>Accueil</h1>
    <img src="{{ asset('img/api.png') }}" width="100px">
</div>

<div>
    <a href="{{ path('app_burger') }}">Liste des burgers</a><br><br>

    <a href="{{ path('app_bread') }}">Liste des pains</a><br>
    <a href="{{ path('app_onion') }}">Liste des oignons</a><br>
    <a href="{{ path('app_sauce') }}">Liste des sauces</a><br>
    <a href="{{ path('app_image') }}">Liste des images</a><br>
    <a href="{{ path('app_review') }}">Liste des commentaires</a>
</div>

{% endblock %}
